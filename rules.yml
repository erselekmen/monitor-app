groups:
  - name: error_alerts
    rules:
      - alert: HighErrorRate
        expr: (rate(error_count[5m]) / rate(request_count[5m])) > 0.1
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "High error rate detected"